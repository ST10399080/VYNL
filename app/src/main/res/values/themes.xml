<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.Vynl_app" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <style name="Theme.Vynl_app" parent="Base.Theme.Vynl_app">
        <!-- Matches VynlColors.Background so there's no white flash on cold start. -->
        <item name="android:windowBackground">@color/vynl_background</item>
    </style>

</resources>